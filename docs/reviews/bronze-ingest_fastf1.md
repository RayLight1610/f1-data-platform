## BLOCKER
- `src/f1_platform/bronze/ingest_fastf1.py:51,75` (same pattern at 79/103, 107/131, 135/169) — `mode` is a public parameter forwarded straight into `to_sql(if_exists=mode)`. Calling any `ingest_race_*` with `mode="replace"` to reload one event drops and recreates the *whole* bronze table, wiping every other year/event already loaded → remove the parameter or restrict it to `{"append"}`; if a per-event reload is needed, `DELETE WHERE year=... AND event=...` inside a transaction, then insert.
- `src/f1_platform/bronze/ingest_fastf1.py:29-48,74-76` (same in 102-104, 130-132, 168-170) — `already_ingested` only checks that *any* row exists for `(year, event)`. `to_sql` is not transactional; if the process is killed mid-write, partial rows remain, and the next run's `already_ingested` check sees them and skips forever — the event can never be completed or repaired without manual SQL. Wrap the check + delete-if-partial + insert in one transaction (`engine.begin()`), or use a staging table swapped in atomically.
- `src/f1_platform/bronze/ingest_fastf1.py:148-155` — `session.laps.pick_fastest()` returns an empty/all-NaN result for a red-flagged, no-time-set, or fully-deleted-laps session; `fastest_lap["Driver"]` then raises `KeyError`/`AttributeError`. Uncaught here (only `load_session` is try/except'd), this crashes any direct call to `ingest_race_telemetry` outside `ingest_season`. Check `fastest_lap.empty` (or equivalent) before indexing and skip with a logged reason.
- `src/f1_platform/bronze/ingest_fastf1.py:173-184` — `ingest_season` calls `load_session` (network + full session parse) up to 4 separate times per race (once each from `ingest_race_laps/results/weather/telemetry`), and on a 429 there is no backoff — the loop just moves to the next call/event and retries the same exhausted quota. Against the documented ~200 req/hour budget this burns the limit 4x faster than necessary and will rate-limit-storm the rest of the season. Load the session once per event and pass it into the four ingest functions; add exponential backoff and stop-the-run behavior on 429.

## WARNING
- `src/f1_platform/bronze/ingest_fastf1.py:58-62` (repeated at 86-90, 114-118, 142-146) — `except Exception` logs and returns/continues for every error class (auth failure, malformed cache, schema change, `KeyboardInterrupt`-adjacent). Real bugs get silently absorbed as "skipped race". Catch specific FastF1/network exceptions; re-raise or surface anything unexpected.
- `src/f1_platform/bronze/ingest_fastf1.py:53,74,81,102,109,130,137,168` — `get_engine()` is called fresh in every function (2x per ingest call), creating a new `create_engine()`/connection pool each time; none is ever `.dispose()`d. Over `ingest_season` this is dozens of undisposed pools per run. Create one engine per process/run and pass it in, or dispose after use.
- `src/f1_platform/bronze/ingest_fastf1.py:45` — table name is interpolated into SQL via f-string (`f"SELECT 1 FROM bronze.{table}..."`). Currently only fed internal constants, but it's an injection-shaped pattern that will bite the first time this helper is reused with a variable table name. Use an allow-list or `sqlalchemy.sql.quoted_name`/identifier binding.
- `src/f1_platform/bronze/ingest_fastf1.py:29-48,75` — check-then-write with no unique constraint on the bronze tables: two overlapping runs (or a retry that races the first run's write) both pass `already_ingested`, then both insert → silent duplicate rows, no PK violation to catch it. Add a unique constraint on `(year, event[, driver/lap grain])` or use `ON CONFLICT DO NOTHING` semantics via a staged upsert.
- `src/f1_platform/bronze/ingest_fastf1.py:25` — `datetime.now()` is naive local time, not UTC, for `ingested_at`. Depends on server locale/TZ and is inconsistent with the project's UTC timestamp convention. Use `datetime.now(timezone.utc)`.
- `src/f1_platform/bronze/ingest_fastf1.py:75,103,131,169` — first `to_sql` call per table infers column types from whatever DataFrame happens to load first; a later season/event with new, renamed, or missing FastF1 columns (e.g., sprint-related fields) will raise on `append` (column mismatch) and be swallowed by the `except Exception` in `ingest_season`, silently skipping that event forever with no alert. Define explicit bronze DDL under `sql/bronze/` and use `dtype=` on `to_sql`, or validate the incoming DataFrame's columns against the existing table before writing.

## NIT
- `src/f1_platform/bronze/ingest_fastf1.py:24-26` — `add_metadata_columns` mutates its argument in place and returns `None`; callers reassign nothing (`add_metadata_columns(laps)` not `laps = ...`), which works only because of the hidden mutation — fragile if pandas' copy-on-write semantics change. Return the DataFrame explicitly.
- `src/f1_platform/bronze/ingest_fastf1.py:135` — `ingest_race_telemetry` only ever captures the fastest lap's telemetry, not the race's telemetry; the name overpromises scope for future readers/callers.

## VERDICT: FAIL
Non-atomic idempotency check can strand partial ingests permanently, `mode="replace"` can wipe the whole bronze table, and telemetry ingestion crashes on cancelled/no-valid-lap sessions.
